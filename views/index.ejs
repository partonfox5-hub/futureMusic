<%- include('partials/header') %>

<!-- HERO SECTION -->
<!-- Added relative and overflow-hidden to contain the background elements -->
<section id="hero-section" class="min-h-[85vh] flex flex-col items-center justify-center text-center relative overflow-hidden perspective-1000">
    
    <!-- PARALLAX BACKGROUND IMAGES -->
    <!-- These sit at z-0, behind the z-10 content -->
    <div class="absolute inset-0 w-full h-full pointer-events-none z-0 overflow-hidden">
        <!-- Image 1: Left Side (Stairs) -->
        <!-- "Way in the background" achieved via opacity-30 and scale-75 -->
        <div class="parallax-layer absolute top-1/4 -left-12 md:left-20 opacity-30 grayscale hover:grayscale-0 transition-all duration-700" data-speed="-0.02">
            <div class="transform -rotate-6 p-2 bg-gray-900 shadow-2xl border border-[#D4AF37]/30">
                <img src="/images/Screenshot 2025-12-14 220503.jpg" alt="Background Art 1" class="w-64 md:w-96 h-auto block opacity-80 mix-blend-screen">
            </div>
        </div>

        <!-- Image 2: Right Side (City/Game) -->
        <div class="parallax-layer absolute bottom-1/4 -right-12 md:right-20 opacity-30 grayscale hover:grayscale-0 transition-all duration-700" data-speed="0.03">
            <div class="transform rotate-3 p-2 bg-gray-900 shadow-2xl border border-[#D4AF37]/30">
                <img src="/images/Screenshot 2025-12-14 220350.jpg" alt="Background Art 2" class="w-64 md:w-80 h-auto block opacity-80 mix-blend-screen">
            </div>
        </div>
    </div>

    <!-- MAIN CONTENT CONTAINER -->
    <!-- z-10 ensures this sits ON TOP of the images -->
    <div class="max-w-4xl mx-auto relative z-10">
        <div class="mb-8 relative inline-block">
            <!-- UPDATED: Sun raised higher above text (-top-32) and removed downward transform -->
            <div class="absolute inset-x-0 -top-32 flex justify-center text-[#B8860B] animate-pulse" style="animation-duration: 6s;">
                <i class="fas fa-sun text-[10rem] drop-shadow-[0_0_20px_rgba(184,134,11,0.6)]" style="
                    font-size: 8rem; 
                "></i>
            </div>
            
            <h1 class="text-6xl md:text-8xl font-black text-transparent bg-clip-text bg-gradient-to-b from-[#F4C430] to-[#AA8C2C] drop-shadow-2xl mb-4 relative z-10">
                THINK FREELY
            </h1>
            <h2 class="text-2xl md:text-4xl text-white font-light tracking-[0.5em] mb-8">AS ONE AND THE SAME</h2>
        </div>
        
        <p class="text-gray-300 text-lg md:text-xl max-w-2xl mx-auto mb-12 leading-relaxed drop-shadow-md bg-black/20 backdrop-blur-sm rounded-lg p-4">
            Wordplay into the embrace of richer mindhood and such things as that 
        </p>

        <div class="flex flex-col sm:flex-row gap-6 justify-center">
            <a href="/music" class="btn-gold px-8 py-4 text-lg rounded-sm shadow-lg inline-block">
                Stream Now
            </a>
            <a href="/projects" class="btn-outline px-8 py-4 text-lg rounded-sm inline-block">
                View Works
            </a>
        </div>
    </div>
    
    <div class="absolute bottom-8 animate-pulse text-[#D4AF37] z-10">
        <i class="fas fa-chevron-down text-3xl"></i>
    </div>
</section>

<!-- EXTENDED SECTIONS -->

<!-- 1. MUSIC SYNOPSIS -->
<section class="py-20 border-t border-yellow-900/30">
    <div class="max-w-6xl mx-auto grid grid-cols-1 md:grid-cols-2 gap-12 items-center">
        <div class="order-2 md:order-1">
            <div class="relative w-full aspect-video bg-gray-900 rounded-lg border border-[#D4AF37]/50 overflow-hidden flex items-center justify-center group">
                <!-- Abstract visual representation of sound -->
                <div class="absolute inset-0 bg-[url('https://www.transparenttextures.com/patterns/cubes.png')] opacity-20"></div>
                <i class="fas fa-broadcast-tower text-6xl text-[#D4AF37] group-hover:scale-125 transition-transform duration-500"></i>
            </div>
        </div>
        <div class="order-1 md:order-2 space-y-6">
            <h2 class="text-4xl text-gold">AUDITORY COMMANDS</h2>
            <p class="text-gray-300 leading-loose text-lg">
                Our sonic output is not merely "music"—it is a structured frequency designed to reorder the subconscious. 
                Drawing from the raw energy of psychedelic rock and the disciplined structure of orchestral composition, 
                we create a soundscape where chaos and control coexist.
            </p>
            <ul class="space-y-2 text-gray-400">
                <li><i class="fas fa-check text-[#20B2AA] mr-2"></i> High-Fidelity Psychedelia</li>
                <li><i class="fas fa-check text-[#20B2AA] mr-2"></i> Symphonic Arrangements</li>
                <li><i class="fas fa-check text-[#20B2AA] mr-2"></i> Lyricism as Doctrine</li>
            </ul>
            <a href="/music" class="inline-block mt-4 text-[#D4AF37] border-b border-[#D4AF37] pb-1 hover:text-white transition-colors">
                INSPECT DISCOGRAPHY &rarr;
            </a>
        </div>
    </div>
</section>

<!-- 2. PROJECTS SYNOPSIS -->
<section class="py-20 border-t border-yellow-900/30 bg-black/40">
    <div class="max-w-6xl mx-auto grid grid-cols-1 md:grid-cols-2 gap-12 items-center">
        <div class="space-y-6">
            <h2 class="text-4xl text-gold">VISUAL PROPAGANDA</h2>
            <p class="text-gray-300 leading-loose text-lg">
                The message must be seen to be believed. Our visual archives document the aesthetic 
                of the new order. From liquid light shows that mimic the melting of reality to 
                the stark, authoritarian lines of our uniform collection.
            </p>
            <p class="text-gray-400">
                <em>"The eye is the window to the compliant soul."</em>
            </p>
            <a href="/projects" class="inline-block mt-4 text-[#D4AF37] border-b border-[#D4AF37] pb-1 hover:text-white transition-colors">
                VIEW ARCHIVES &rarr;
            </a>
        </div>
        <div>
            <div class="relative w-full aspect-video bg-gray-900 rounded-lg border border-[#D4AF37]/50 overflow-hidden flex items-center justify-center group">
                <div class="absolute inset-0 bg-[url('https://www.transparenttextures.com/patterns/diagmonds-light.png')] opacity-20"></div>
                <i class="fas fa-eye text-6xl text-[#20B2AA] group-hover:rotate-180 transition-transform duration-700"></i>
            </div>
        </div>
    </div>
</section>

<!-- 3. CAPITALISM / MERCH SYNOPSIS -->
<section class="py-20 border-t border-yellow-900/30">
    <div class="max-w-4xl mx-auto text-center">
        <div class="mb-6">
            <i class="fas fa-coins text-5xl text-[#D4AF37] mb-4"></i>
            <h2 class="text-4xl text-gold">CAPITALISM & CONTROL</h2>
        </div>
        
        <p class="text-gray-300 leading-loose text-lg mb-8">
            We exist within the system to dismantle it—or perhaps, to perfect it. 
            In this transactional reality, your support fuels the machine. 
            We offer tangible assets: uniforms, physical media, and artifacts of the Mystic One. 
            Exchange your currency for compliance. 
        </p>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12">
            <div class="glass-card p-6 rounded text-center">
                <i class="fas fa-tshirt text-3xl mb-3 text-gray-500"></i>
                <h4 class="font-bold text-[#D4AF37]">UNIFORMS</h4>
                <p class="text-xs text-gray-400 mt-2">Apparel for the ranks.</p>
            </div>
            <div class="glass-card p-6 rounded text-center">
                <i class="fas fa-record-vinyl text-3xl mb-3 text-gray-500"></i>
                <h4 class="font-bold text-[#D4AF37]">ARTIFACTS</h4>
                <p class="text-xs text-gray-400 mt-2">Vinyl & Physical Audio.</p>
            </div>
            <div class="glass-card p-6 rounded text-center">
                <i class="fas fa-ticket-alt text-3xl mb-3 text-gray-500"></i>
                <h4 class="font-bold text-[#D4AF37]">ACCESS</h4>
                <p class="text-xs text-gray-400 mt-2">Live Ritual Entry.</p>
            </div>
        </div>

        <button class="btn-gold px-12 py-3 rounded-sm font-bold shadow-lg" onclick="alert('Store module loading...')">
            ENTER THE EXCHANGE
        </button>
    </div>
</section>

<script>
    // Parallax Effect Script
    document.addEventListener('mousemove', (e) => {
        const layers = document.querySelectorAll('.parallax-layer');
        
        const mouseX = e.clientX;
        const mouseY = e.clientY;
        
        // Calculate center of screen
        const centerX = window.innerWidth / 2;
        const centerY = window.innerHeight / 2;
        
        layers.forEach(layer => {
            const speed = layer.getAttribute('data-speed');
            
            // Calculate distance from center
            const x = (mouseX - centerX) * speed;
            const y = (mouseY - centerY) * speed;
            
            // Apply transformation
            layer.style.transform = `translateX(${x}px) translateY(${y}px)`;
        });
    });
</script>

<%- include('partials/footer') %>