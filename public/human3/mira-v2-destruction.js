import * as T from 'three';
const V=()=>new T.Vector3(),Q=()=>new T.Quaternion();
const MATERIALS={plaster:{health:48,density:650,color:0xc9c1b1},wood:{health:70,density:600,color:0x806047},glass:{health:11,density:2500,color:0x9fc1c7},stone:{health:210,density:2400,color:0x85847c},metal:{health:190,density:7800,color:0x929b9d}};
export class Destruction {
 constructor(scene,world){Object.assign(this,{scene,world});this.parts=[];this.max=/Quest|OculusBrowser/i.test(globalThis.navigator?.userAgent||'')?48:96;this.fragments=[];this.mesh=new T.InstancedMesh(new T.BoxGeometry(1,1,1),new T.MeshStandardMaterial({roughness:.88}),this.max);this.mesh.frustumCulled=false;this.mesh.instanceMatrix.setUsage(T.DynamicDrawUsage);scene.add(this.mesh);this.matrix=new T.Matrix4();for(let i=0;i<this.max;i++)this.mesh.setMatrixAt(i,this.matrix.makeScale(0,0,0));this.slot=0;}
 clear(){this.parts=[];this.fragments=[];for(let i=0;i<this.max;i++)this.mesh.setMatrixAt(i,this.matrix.makeScale(0,0,0));this.mesh.instanceMatrix.needsUpdate=true;}
 panel(center,size,kind='plaster',hole=()=>false){const axis=['x','y','z'].sort((a,b)=>size[a]-size[b])[0],axes=['x','y','z'].filter(a=>a!==axis),u=axes[0],v=axes[1],nu=Math.ceil(size[u]/.60),nv=Math.ceil(size[v]/.60),cells=[];for(let a=0;a<nu;a++)for(let b=0;b<nv;b++){const p=center.clone(),s=size.clone();s[u]=size[u]/nu;s[v]=size[v]/nv;p[u]+=((a+.5)/nu-.5)*size[u];p[v]+=((b+.5)/nv-.5)*size[v];if(!hole(p))cells.push({p,s});}
 const map=kind==='plaster'?this.world.tex('plaster_wall.jpg'):kind==='wood'?this.world.tex('walnut_wood.jpg'):null;if(map){map.wrapS=map.wrapT=T.RepeatWrapping;map.repeat.set(kind==='plaster'?2.4:1.6,kind==='plaster'?1.6:1.2);}
 const mat=new T.MeshStandardMaterial({color:map?0xffffff:MATERIALS[kind].color,map:kind==='glass'?null:map,roughness:kind==='glass'?.18:.9,transparent:kind==='glass',opacity:kind==='glass'?.30:1,metalness:0}),mesh=new T.InstancedMesh(new T.BoxGeometry(1,1,1),mat,cells.length);mesh.name=kind+' wall';mesh.castShadow=mesh.receiveShadow=true;mesh.userData.chunks=[];this.world.root.add(mesh);this.world.pickables.push(mesh);
 cells.forEach(({p,s},i)=>{mesh.setMatrixAt(i,new T.Matrix4().compose(p,Q(),s));const o=this.world.obstacle(p.x,p.z,s.x,s.z,p.y-s.y/2,s.y);const part={mesh,index:i,p,size:s,kind,health:MATERIALS[kind].health,obstacle:o,broken:false};mesh.userData.chunks.push(part);this.parts.push(part);});return mesh;
 }
 register(mesh,kind='wood',obstacle=null){mesh.updateWorldMatrix(true,false);const box=new T.Box3().setFromObject(mesh),part={mesh,p:box.getCenter(V()),size:box.getSize(V()),kind,health:MATERIALS[kind].health,obstacle,broken:false};mesh.userData.piece=part;this.parts.push(part);if(!this.world.pickables.includes(mesh))this.world.pickables.push(mesh);return part;}
 impact(hit,energy,dir,kind='blunt',sharpness=0){const part=hit.object.userData.chunks?.[hit.instanceId]||hit.object.userData.piece;if(!part||part.broken)return false;const multiplier=kind==='cut'?(part.kind==='wood'?1+sharpness:part.kind==='plaster'?.85:.28):kind==='laser'?1.4:1;part.health-=Math.max(0,energy)*multiplier;if(part.health<=0)this.break(part,dir,energy);return part.broken;}
 break(part,dir,energy){if(part.broken)return;part.broken=true;if(part.index!==undefined){part.mesh.setMatrixAt(part.index,new T.Matrix4().makeScale(0,0,0));part.mesh.instanceMatrix.needsUpdate=true;}else part.mesh.visible=false;
  if(part.obstacle)this.world.removeObstacle(part.obstacle);const seat=part.mesh.userData.seat;if(seat){if(seat.occupant){const a=seat.occupant;a.seat=null;a.group.position.y=a.baseY||0;a.knockDown?.(dir);seat.occupant=null;}this.world.removeObstacle(seat.obstacle);this.world.seats=this.world.seats.filter(s=>s!==seat);seat.group.traverse(m=>{delete m.userData.seat;});}
  for(let i=0;i<6;i++){const offset=new T.Vector3((i%2-.5)*part.size.x*.55,((i>>1)%2-.5)*part.size.y*.55,((i>>2)%2-.5)*part.size.z*.55),velocity=dir.clone().multiplyScalar(Math.min(4,Math.sqrt(Math.max(0,energy))*.24)).add(new T.Vector3(Math.sin(i*2.4),.6+(i%3)*.22,Math.cos(i*2.4)));this.fragment(part.p.clone().add(offset),part.size.clone().multiplyScalar(.35),MATERIALS[part.kind].color,velocity);}
 }
 fragment(position,size,color,velocity){const i=this.slot++%this.max;this.fragments=this.fragments.filter(f=>f.i!==i);this.fragments.push({i,p:position.clone(),s:size.clone().clamp(new T.Vector3(.008,.008,.008),new T.Vector3(.38,.38,.38)),v:velocity.clone(),q:Q(),spin:new T.Vector3(Math.random(),Math.random(),Math.random()).multiplyScalar(5),age:0});this.mesh.setColorAt(i,new T.Color(color));this.mesh.instanceColor.needsUpdate=true;}
 tick(dt){dt=Math.min(.04,dt);for(const f of this.fragments){f.age+=dt;f.v.y-=9.81*dt;const steps=Math.max(1,Math.ceil(f.v.length()*dt/.06));for(let k=0;k<steps;k++){const old=f.p.clone();f.p.addScaledVector(f.v,dt/steps);if(this.world.projectSphere(f.p,.5*Math.min(f.s.x,f.s.y,f.s.z))){const normal=f.p.clone().sub(old).normalize(),vn=f.v.dot(normal);if(vn<0)f.v.addScaledVector(normal,-1.15*vn);f.v.multiplyScalar(.82);}}if(f.p.y<f.s.y/2){f.p.y=f.s.y/2;f.v.y=Math.abs(f.v.y)*.18;f.v.x*=.9;f.v.z*=.9;}f.q.multiply(new T.Quaternion().setFromEuler(new T.Euler(f.spin.x*dt,f.spin.y*dt,f.spin.z*dt)));this.mesh.setMatrixAt(f.i,new T.Matrix4().compose(f.p,f.q,f.s.clone().multiplyScalar(f.age>8?Math.max(0,9-f.age):1)));}this.fragments=this.fragments.filter(f=>f.age<9);this.mesh.instanceMatrix.needsUpdate=true;}
}
