import * as T from 'three';
import {bodyVolumes,projectVolume} from './mira-v2-contact.js?v=h3.2';
const V=()=>new T.Vector3(),Q=()=>new T.Quaternion(),up=new T.Vector3(0,1,0);
export class Restraints {
 constructor(props){this.props=props;this.scene=props.scene;this.system=props.system;this.world=props.world;this.links=[];this.selected=null;this.pending=null;this.placing=false;this.mode='rope';this.button=new WeakMap();this.next=1;this.status='Click PLACE, then choose two anchor points.';}
 anchor(hit){const actor=this.props.actorFor(hit.object),point=hit.point.clone();if(actor){if(actor.version!=='v2')return null;const region=actor.nearestHit(point,.22);if(!region)return null;const bone=actor.bones[region.name];return {actor,bone,region,local:bone.worldToLocal(point.clone()),point,weight:1/70};}const item=this.props.items.find(i=>i.id===hit.object.userData.weapon);if(item)return {object:item.group,item,local:item.group.worldToLocal(point.clone()),point,weight:1/item.data.mass};const piece=hit.object.userData.chunks?.[hit.instanceId]||hit.object.userData.piece;if(hit.object.isInstancedMesh)return {point,piece,weight:0};const car=hit.object.userData.carPart;return {object:hit.object,local:hit.object.worldToLocal(point.clone()),point,piece,car,weight:car?1/1250:0};}
 position(a){if(a.actor)return a.bone.localToWorld(a.local.clone());if(a.object)return a.object.localToWorld(a.local.clone());return a.point.clone();}
 start(){this.placing=true;this.pending=null;this.status='Choose first anchor.';}
 cancel(){this.pending=null;this.placing=false;this.status='Placement cancelled.';}
 place(ray){const old=this.hit(ray,15);let hit=this.props.hit(ray,15,false);const rc=new T.Raycaster();rc.ray.copy(ray);rc.far=hit?.distance||15;const equipment=rc.intersectObjects(this.props.items.map(i=>i.group),true)[0];if(equipment)hit=equipment;if(old&&(!hit||old.distance<hit.distance)){this.selected=old.object.userData.restraint;this.status='Selected link '+this.selected.id;return true;}if(!hit){this.status='Point at an NPC, object or wall.';return true;}const anchor=this.anchor(hit);if(!anchor){this.status='Choose a v2 actor or scenery.';return true;}if(!this.pending){this.pending=anchor;this.placing=true;this.status='First anchor set. Choose the second.';return true;}const a=this.pending;if(a.bone===anchor.bone&&a.object===anchor.object&&this.position(a).distanceTo(this.position(anchor))<.05){this.status='Choose a different anchor point.';return true;}this.create(a,anchor,this.mode);this.pending=null;this.placing=false;return true;}
 create(a,b,mode='rope'){if(this.links.length>=8)this.remove(this.links[0]);const p=this.position(a),q=this.position(b),length=mode==='fuse'?.03:Math.max(.08,p.distanceTo(q)*1.04),n=12,rope=new T.InstancedMesh(new T.CylinderGeometry(.006,.006,1,7),new T.MeshStandardMaterial({color:mode==='fuse'?0xb5bdc4:0x8c7454,roughness:.8,metalness:mode==='fuse'?.6:0}),n);rope.frustumCulled=false;const link={id:this.next++,a,b,length,mode,n,rope,broken:false,cut:6,p:[],prev:[],cuffs:[]};rope.userData.restraint=link;for(let i=0;i<=n;i++){const v=p.clone().lerp(q,i/n);link.p.push(v);link.prev.push(v.clone());}for(const end of [a,b]){const r=end.actor?end.region.kind==='head'?.061:/thigh|hip/.test(end.region.kind)?.086:/arm|hand|foot|leg/.test(end.region.kind)?.038:.10:.025,cuff=new T.Mesh(new T.TorusGeometry(r,.006,6,24),new T.MeshStandardMaterial({color:0xa5b0b6,roughness:.32,metalness:.6}));cuff.userData.restraint=link;this.scene.add(cuff);link.cuffs.push(cuff);}this.scene.add(rope);this.links.push(link);this.selected=link;this.status='Link '+link.id+' attached. Adjust length or cut/remove.';return link;}
 setLength(value){if(this.selected&&!this.selected.broken)this.selected.length=T.MathUtils.clamp(Number(value)||.03,.03,5);}
 cut(link=this.selected,index=6){if(!link||link.broken)return;link.broken=true;link.cut=T.MathUtils.clamp(index,1,link.n-2);link.rope.material.color.setHex(0x57483a);this.status='Link '+link.id+' cut; loose ends remain.';}
 remove(link=this.selected){if(!link)return;for(const m of [link.rope,...link.cuffs]){m.removeFromParent();m.geometry.dispose();m.material.dispose();}this.links=this.links.filter(l=>l!==link);if(this.selected===link)this.selected=this.links.at(-1)||null;this.status='Link removed.';}
 hit(ray,max=15){const rc=new T.Raycaster();rc.ray.copy(ray);rc.far=max;return rc.intersectObjects(this.links.flatMap(l=>[l.rope,...l.cuffs]),false)[0];}
 move(a,delta,dt){if(a.item){if(a.item.holder===null){a.item.group.position.add(delta.clone().clampLength(0,.06));a.item.velocity.multiplyScalar(.8);}return;}if(a.car){this.props.vehicle.pull(delta);return;}const actor=a.actor;if(!actor||!this.system.actors.includes(actor))return;actor.socialPair?.cancel();actor.autonomy=false;actor.dest=null;actor.navigation=null;actor.seat=null;const name=a.region.name,side=name[0],arm=/arm|hand/.test(a.region.kind),leg=/thigh|leg|foot/.test(a.region.kind),correction=delta.clone().clampLength(0,Math.min(.055,dt*2.5));if(arm||leg){const kind=arm?'arm':'leg',tip=actor.bones[side+(arm?'_Hand':'_Foot')],pole=actor.group.localToWorld(new T.Vector3((side==='L'?1:-1)*(arm?.40:.14),arm?1.12:.50,arm?-.15:.35).multiplyScalar(actor.shape.height));actor.solveChain(side,kind,tip.getWorldPosition(V()).add(correction),pole);actor.group.updateMatrixWorld(true);const remaining=delta.length()-.025;if(remaining>.02)actor.group.position.addScaledVector(correction,.35);if(leg&&delta.length()>.12)actor.knockDown(delta);}else{actor.group.position.addScaledVector(correction,.8);if(a.region.kind==='head'){const local=correction.clone().applyQuaternion(actor.group.quaternion.clone().invert());actor.headTouchGoal.add(new T.Vector3(-local.y*2,local.x*2,0));}else actor.spineGoal.add(new T.Vector3(correction.z*.8,0,-correction.x*.8));}actor.group.position.y=Math.max(actor.baseY||0,actor.group.position.y);actor.group.updateMatrixWorld(true);actor.contactSoft?.(a.region,delta.clone().normalize(),.002,.03);}
 solve(dt){for(const link of this.links){if(link.broken)continue;if([link.a,link.b].some(a=>a.piece?.broken||a.actor&&!this.system.actors.includes(a.actor))){this.cut(link);continue;}for(let j=0;j<3;j++){const a=this.position(link.a),b=this.position(link.b),d=b.sub(a),n=d.length(),extra=n-link.length,wa=link.a.item?.holder!=null?0:link.a.weight,wb=link.b.item?.holder!=null?0:link.b.weight,total=wa+wb;if(extra<=0||!total||n<1e-6)break;d.multiplyScalar(extra/n);this.move(link.a,d.clone().multiplyScalar(wa/total),dt/3);this.move(link.b,d.clone().multiplyScalar(-wb/total),dt/3);}}}
 tick(dt){const session=this.props.renderer.xr.getSession?.();if(session&&!this.props.menu?.isOpen)for(const src of session.inputSources){if(src.handedness!=='left'||!src.gamepad)continue;const down=!!src.gamepad.buttons[3]?.pressed;if(down&&!this.button.get(src)){const index=this.system.hands.handedness.indexOf('left');this.place(this.props.ray(index<0?0:index));}this.button.set(src,down);}const volumes=this.system.actors.filter(a=>a.version==='v2').flatMap(bodyVolumes);for(const l of this.links){const a=this.position(l.a),b=this.position(l.b);for(let i=0;i<=l.n;i++){const p=l.p[i],old=p.clone();p.add(p.clone().sub(l.prev[i]).multiplyScalar(Math.exp(-dt*3)));p.y-=9.81*dt*dt;l.prev[i].copy(old);}const segment=l.length/l.n;for(let pass=0;pass<4;pass++){l.p[0].copy(a);l.p[l.n].copy(b);for(let i=0;i<l.n;i++){if(l.broken&&i===l.cut)continue;const d=l.p[i+1].clone().sub(l.p[i]),len=d.length();if(len<1e-6)continue;d.multiplyScalar((len-segment)/len);if(i===0)l.p[i+1].sub(d);else if(i===l.n-1)l.p[i].add(d);else{l.p[i].addScaledVector(d,.5);l.p[i+1].addScaledVector(d,-.5);}}}for(let i=1;i<l.n;i++){for(const c of volumes)projectVolume(l.p[i],.005,c);this.world.projectSphere(l.p[i],.006);l.p[i].y=Math.max(.008,l.p[i].y);}l.p[0].copy(a);l.p[l.n].copy(b);for(let i=0;i<l.n;i++){const p=l.p[i],q=l.p[i+1],d=q.clone().sub(p),length=d.length(),m=new T.Matrix4();if(l.broken&&i===l.cut)m.makeScale(0,0,0);else m.compose(p.clone().lerp(q,.5),Q().setFromUnitVectors(up,d.normalize()),new T.Vector3(1,Math.max(.001,length),1));l.rope.setMatrixAt(i,m);}l.rope.instanceMatrix.needsUpdate=true;l.rope.computeBoundingSphere();l.cuffs.forEach((c,i)=>{const end=i?l.b:l.a,p=i?b:a;c.position.copy(p);if(end.bone)c.quaternion.copy(end.bone.getWorldQuaternion(Q()));else c.lookAt(i?a:b);});}}
}
